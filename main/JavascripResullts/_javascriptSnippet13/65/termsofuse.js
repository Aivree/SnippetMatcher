'use strict';

angular.module('appApp')
  .controller('TermsofuseCtrl',["$scope", "Nav",
   function ($scope, Nav) {
      init();

      function init() {
      }

      $scope.acceptTerms = function () {
          

      }
  }]);
